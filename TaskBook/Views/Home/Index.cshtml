@model IEnumerable<TaskBook.Models.MyModel>
@{
    ViewBag.Title = "Home Page";
}

@section ul
    {
    @foreach (var x in Model)
    {
        @Html.Partial("UlMethod", x)
    }
}

   
    <form class="Myform" action="@Url.Action("About")" method="post" enctype="multipart/form-data">
        <p>Choose File .pdf</p>
        <input  class="MyformInput" type="file" name="file"  required="required" />@ViewBag.Message1<br />
        <p>Choose image</p>
        <input  class="MyformInput" type="file" name="nkar" required="required" /> @ViewBag.Message2  <br />
        <p>Enter the author's name and title of the book</p>
        <input class="MyformInput" type="text" name="vernagir" size="60" maxlength="40" required="required" /><br />
        <p>about the book</p>
        <input class="MyformInput2" type="text" name="nkaragir" size="120" maxlength="60" required="required" /><br /><br />

        <input class="MyformInput1" type="submit" value="Add" />
    </form>

